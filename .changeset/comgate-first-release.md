---
"@medusa-cz/payment-comgate": minor
---

First release. Comgate payment provider for Medusa v2: redirect checkout flow,
automatic and manual (pre-auth) capture modes, unsigned-webhook handling (always
re-queries status rather than trusting the payload). TDD-built, 25 unit tests.
